<p class="invoice_buttons">
	<a class='btn' href="<%= invoice_path(@order.number, :format => 'pdf') %>"><i class='icon-download-alt'></i> Download Invoice PDF</a>
	<a class='btn' href="<%= invoice_path(@order.number, :print => true) %>" target="_blank"><i class='icon-print'></i> Print Invoice</a>
</p>
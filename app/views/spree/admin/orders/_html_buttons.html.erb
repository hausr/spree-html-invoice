<% (Spree::HtmlInvoice::Config[:print_buttons] || "invoice").split(",").map(&:strip).each do |button| %>
<%= button_link_to(t("#{button}.print"), "/admin/invoice/#{@order.number}/#{button}", :target => "_blank") %>
<% end %>

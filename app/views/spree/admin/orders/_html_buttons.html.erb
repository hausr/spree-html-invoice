<% (Spree::HtmlInvoice::Config[:print_buttons] || "invoice").split(",").map(&:strip).each do |button| %>
<%= button_link_to(t("#{button}.print"), invoices_path(@order), :target => "_blank") %>
<% end %>
